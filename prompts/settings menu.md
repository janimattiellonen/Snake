# Settings menu


## Dark mode

Implement a basic dark mode, where the playing are background is either light (current background) or dark 
(darker color, but a color that differs from the pahe background color).

Make the dark mode configurable from a settings menu (similar to help menu).

Title for this menu is "Settings". Store the user selection to localstorage and retrieve it from there when
the game game page loads.